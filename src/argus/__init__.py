"""ARGUS: auditing causal identification in DID environmental-policy evaluation.

Bounded-agentic system. The audit pipeline runs a fixed, deterministic sequence
of stages; only `extraction` and `assessment` invoke a bounded agent loop.
"""

__version__ = "0.1.0"
