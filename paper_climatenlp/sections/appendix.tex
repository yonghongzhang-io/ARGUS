\section{Domain Motivation and Design Rationale}
\label{app:motivation}

\subsection{Why climate-policy DID is a demanding audit setting}
Climate-policy evaluations present several recurring identification challenges.
First, policy announcement, legal adoption, and actual implementation may occur at
different dates, creating scope for anticipation. Second, carbon markets and
environmental regulations are commonly introduced at different times across
jurisdictions, making heterogeneous and staggered treatment effects central rather
than exceptional. Third, emissions, investment, and production responses can spill
across regional borders, challenging no-interference assumptions. Fourth, climate
policies frequently overlap with industrial, energy, and air-pollution regulations,
complicating the exclusion of concurrent interventions. Fifth, policy effects may
emerge with long and heterogeneous lags. Finally, emissions and environmental
outcomes are assembled from administrative, modeled, or remotely sensed sources
whose measurement properties vary across places and time. These features do not make
DID unique to climate policy, but they make the quality of identification evidence
especially consequential for policy interpretation and comparison.

\subsection{Identification auditing and research reliability}
\sys{} addresses one component of a broader research-reliability problem: published
conclusions can vary with specification choices, undisclosed analytic flexibility,
and researcher degrees of freedom
\cite{leamer1983,ioannidis2005,simmons2011,gelmanloken2014,silberzahn2018many,brodeur2020methods,botviniknezer2020variability}.
This is the identification-credibility component of the broader research-design or
``credibility revolution'' program in applied econometrics
\cite{angristpischke2010,atheyimbens2017}, which increasingly treats an assumption
like parallel trends as a matter of degree rather than a binary
\cite{rambachanroth2023,roth2022}. \sys{} does not attempt to audit every component
of an empirical paper; it focuses on the identification argument that licenses a
causal interpretation.

\subsection{Why \sys{} uses bounded agency}
Open-ended agents may choose different tools and reasoning paths across runs, making
their outputs difficult to reproduce and compare. Large language models are
increasingly used for evaluative and reviewing tasks
\cite{zheng2023judge,liang2024feedback,lu2024aiscientist}, but as open-ended agents
their reasoning traces are hard to audit. \sys{} instead fixes the pipeline order in
code and confines model-driven decisions to evidence retrieval and adequacy
assessment. Each model call operates under a fixed tool set, a hard step budget, and
a logged trace. This design trades autonomy for reproducibility, inspectability, and
controlled evaluation under flaw injection.

\section{Reproducibility Details}
\label{app:repro}

All LLM stages use \texttt{gpt-4o} at temperature~$0$ with
\texttt{max\_steps}{=}1. The identification rubric, flaw taxonomy, and
33-variant catalog are declarative configuration files in the code release, and
the keyword baseline plus all leak-freedom (non-circularity) checks are
deterministic and run without model access. The eleven-flaw headline numbers are
the frozen output of the released runner. The 33-variant benchmark was run three
times end-to-end (roughly $750$ model calls per run); verdicts were
near-deterministic (32/33 identical across runs), and Table~\ref{tab:variants}
reports the three-run mean.

\section{The Real-Paper Corpus}
\label{app:corpus}

The 27 published DID studies audited in \S\ref{sec:realpapers} are drawn from
top general-interest and field economics journals
(Table~\ref{tab:corpus}). By topic, the corpus spans finance and banking (5),
environment and energy (5), health (4), education (4), international trade (3),
labour (3), crime and social policy (2), and industrial location (1); the
environment-and-energy subset covers industrial-activity effects of
environmental regulation, air quality and early-life mortality, and energy
pricing. Audit behaviour is not driven by domain: the environment-and-energy
subset's risk distribution (high $0.40$, \texttt{unknown} $0.38$; 55 cells) is
close to the remainder's (high $0.46$, \texttt{unknown} $0.39$; 242 cells).
Paper-level identities are withheld here to preserve double-blind review of the
associated dataset and will be listed in the camera-ready version.

\begin{table}[t]
  \centering\small
  \caption{Journal composition of the 27-paper real corpus.}
  \label{tab:corpus}
  \begin{tabular}{lc}
    \toprule
    journal & papers \\
    \midrule
    Quarterly Journal of Economics & 7 \\
    American Economic Review & 6 \\
    Journal of Political Economy & 4 \\
    Review of Economics and Statistics & 4 \\
    Journal of Finance & 3 \\
    AEJ: Economic Policy / J.\ Monetary Econ.\ / ILRR & 3 \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Qualitative Audit Examples}
\label{app:qual}

\paragraph{A complete audit report.}
Table~\ref{tab:casestudy} shows \sys{}'s actual output on the emissions-trading
fixture with the \emph{anticipation} flaw injected (\S\ref{sec:evaluation}): the
report flags exactly the damaged dimension as high, giving the anticipation-gap
rationale, keeps nine supported dimensions at low, and shows a mild
over-severity trace on \emph{inference} (the fixture does not state the number
of clusters), the same bias the human-gold pilot quantifies. This is the
dimension-level, evidence-cited report a reviewer receives.

\begin{table}[t]
  \centering\small
  \caption{\sys{} audit report (abridged) on the fixture with the injected
  anticipation flaw. Every row cites the evidence it rests on; the injected
  dimension is the one flagged high.}
  \label{tab:casestudy}
  \setlength{\tabcolsep}{4pt}
  \resizebox{\columnwidth}{!}{%
  \begin{tabular}{llp{5.2cm}}
    \toprule
    dimension & risk & rationale (abridged) \\
    \midrule
    no anticipation & \textbf{high} & outcomes began changing between the
    policy announcement and its rollout, undermining no-anticipation \\
    inference & medium & clustering and wild-cluster bootstrap reported, but the
    number of clusters is not stated \\
    parallel trends & low & event-study with null pre-period coefficients and
    placebo tests \\
    treatment timing & low & heterogeneity-robust estimators with a
    Goodman--Bacon decomposition \\
    \emph{7 further dimensions} & low & supporting evidence retrieved and judged
    adequate \\
    \bottomrule
  \end{tabular}}
\end{table}

\paragraph{A grounded judgement on a real paper.}
On a trade-and-wages paper \sys{} retrieves the employment-results section and
flags \emph{inference} as high because ``the paper only mentions
heteroskedastic-consistent standard errors and does not provide information on
clustering or the number of clusters'': a specific econometric observation, not
a keyword hit. Elsewhere it distinguishes a \emph{related but different} reported
check (an alternative-specification section) from the placebo test an assumption
actually requires.

\section{Supplementary Figures and Tables}
\label{app:supp}

This appendix collects supporting material referenced from the main text:
the per-dimension risk distribution on the 27-paper real corpus
(Figure~\ref{fig:realcorpus}), and the deterministic calibration results against
the adjudicated human gold (Table~\ref{tab:calib}, Figure~\ref{fig:calib_tradeoff}),
and the agreement metrics for \sys{} versus the human gold (Table~\ref{tab:vsgold}).

\begin{table}[t]
  \centering\small
  \caption{\sys{}-LLM vs adjudicated human gold (55 cells, 5 papers) under two
  \texttt{unknown} policies: coverage (drop abstentions) and strict
  (\texttt{unknown} counts as mismatch). Pilot-scale: only 2 gold cells are
  \emph{high}; the robust signal is the one-directional over-severity and the
  abstention rate.}
  \label{tab:vsgold}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lcccc}
    \toprule
    \texttt{unknown} policy & $n$ & exact & $\kappa$ & wt.\ $\kappa$ \\
    \midrule
    exclude (coverage) & 33 & 0.152 & 0.019 & 0.044 \\
    mismatch (strict)  & 55 & 0.091 & 0.002 & -- \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/figure_vsgold}
  \caption{\sys{}-LLM versus adjudicated human gold on 55 dimension-level cells
  (5 papers). \textbf{(a)}~coverage, separating answered judgments from
  abstentions; \textbf{(b)}~severity distributions of human gold and \sys{};
  \textbf{(c)}~error direction among the 33 answered cells; \textbf{(d)}~the
  \sys{}-by-gold confusion matrix. The dominant signal is one-directional
  over-severity: \sys{} assigns higher risk than gold in 28/33 answered cases and
  lower risk in none.}
  \label{fig:vsgold}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/figure_realcorpus}
  \caption{Per-dimension risk distribution on 27 real DID papers. The keyword
  baseline (left) is almost entirely \emph{low}; the
  two-stage LLM (right) is differentiated, with a large \texttt{unknown} share
  that marks retrieval failure rather than over-flagging.}
  \label{fig:realcorpus}
\end{figure}

\begin{table}[t]
  \centering\small
  \caption{Deterministic calibration vs the human gold (55 cells), two policies
  for weak-retrieval \emph{high}: demote to \emph{medium} (keep coverage) or
  abstain to \texttt{unknown}. Both cut over-severity and preserve high recall.
  Pilot scale: only 2 gold cells are \emph{high}.}
  \label{tab:calib}
  \setlength{\tabcolsep}{4pt}
  \resizebox{\columnwidth}{!}{%
  \begin{tabular}{lccc}
    \toprule
    metric & before & demote$\to$med & abstain$\to$unk \\
    \midrule
    over-severe (answ.)     & 28 & 18 & 6 \\
    high prec.\ / rec.      & 0.04 / 0.50 & 0.50 / 0.50 & 0.50 / 0.50 \\
    exact agr.\ (answ.)     & 0.15 & 0.45 & 0.54 \\
    wt.\ $\kappa$ (answ.)   & 0.06 & 0.13 & 0.25 \\
    answered / \texttt{unk} & 33 / 22 & 33 / 22 & 13 / 42 \\
    \bottomrule
  \end{tabular}}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/calibration_tradeoff}
  \caption{\textbf{Calibration trade-off: severity reduction versus coverage}
  against the adjudicated human gold. Demoting weak-retrieval \emph{high}s to
  \emph{medium} preserves coverage (33 answered) while cutting over-severe
  judgments from 29 to 18; abstaining gives the strongest correction
  (over-severe 6, weighted $\kappa$ $0.25$) but leaves only 13 answered cells.
  Calibration corrects severity bias; the remaining bottleneck is evidence
  grounding, not severity thresholding alone.}
  \label{fig:calib_tradeoff}
\end{figure}
